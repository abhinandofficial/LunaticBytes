# Data

This directory contains development data.

- `sample/` — small reproducible fixtures/documentation.
- `raw/` — local raw datasets; ignored by Git.
- `processed/` — generated datasets; ignored by Git.

Large rasters and satellite datasets should not be committed directly.
